export {
  DEFAULT_BROWSER_HOST,
  DEFAULT_BROWSER_PORT,
  DEFAULT_TIMEOUT_MS,
  getConfigPath,
  isAllowedChatGptUrl,
  loadConfig,
  resolveProjectAlias,
} from './config.js';
export type { BrowserConfig, ConfigOverrides, FfgptConfig, LoadConfigOptions } from './config.js';
