# ffgpt

`ffgpt` sends prompts to an existing, logged-in ChatGPT session in Firefox and exits after submission. The v0.1 command surface is being implemented; see the CLI help for the current commands.

## Development

```bash
pnpm install
pnpm build
pnpm test
```
